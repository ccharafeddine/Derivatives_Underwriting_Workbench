"""Exposure engine.

The ExposureEngine reprices the netting set across paths and dates into a
net MtM cube and reads off EE, EPE, PFE(95/99), peak PFE, and the exposure cone.
No Qt."""

from __future__ import annotations

# TODO: implement in a later build session (see BUILD_PLAN.md).
