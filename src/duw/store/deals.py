"""Deal-pipeline persistence.

Local persistence (SQLite or JSON) for saved underwriting runs and their
approval stage, with save/load/list/update-stage operations. No Qt required."""

from __future__ import annotations

# TODO: implement in a later build session (see BUILD_PLAN.md).
