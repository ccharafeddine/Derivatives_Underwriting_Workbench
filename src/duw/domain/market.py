"""Market snapshot.

The MarketSnapshot dataclass: yield/discount curves by currency, FX spot
rates, volatilities per risk factor, and CDS spread curves by issuer, as of a
given date. No Qt imports."""

from __future__ import annotations

# TODO: implement in a later build session (see BUILD_PLAN.md).
